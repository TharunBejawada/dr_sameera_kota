# Frontend — Dr. Sameera Kota

Next.js 16 (App Router, SSR), TypeScript. See the [repo root README](../README.md)
for the full monorepo layout and AWS Amplify deployment instructions.

```bash
npm install
npm run dev     # http://localhost:3000
npm run build && npm run start
```
